platform: linux

image_resource:
  type: docker-image
  source:
    repository: pivotalservices/docker-concourse-cf-tools
    tag: "latest"

inputs:
- name: git-assets
- name: mvn-package-output

outputs:
- name: manifest-output

run:
  path: git-assets/pipeline/generate-manifest.sh
